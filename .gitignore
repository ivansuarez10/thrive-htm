node_modules
supabase/.temp/

# Mis archivos de prueba: copias del panel con datos inventados que sirven
# para verlo sin sesión. No son del proyecto y no van al repo, que es
# PÚBLICO. Se colaron una vez con un `git add -A` de otra sesión.
_pv.html
_preview-*.html
_qa*.html

# Pasó dos veces: 86ab99b se llevó _pv.html y b3ca4e7 se llevó _qa.html,
# las dos con `git add -A` desde otra sesión. El patrón cubre lo que
# genere el banco de pruebas, que empieza con guion bajo a propósito.
# comparadores generados: se sirven desde acá para que los iframes carguen,
# pero no son parte del sitio publicado
_cmp*.html
